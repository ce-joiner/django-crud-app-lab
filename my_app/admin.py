from django.contrib import admin
from .models import Tea

# Register your models here.

admin.site.register(Tea)
# This code registers the Tea model with the Django admin site, allowing it to be managed through the admin interface.